# ZoomPaper 后端接口文档

> 面向前端开发的 Tauri 命令对齐文档。所有后端能力通过 `invoke` 暴露，前端无需关心 Rust 实现细节。

## 0. 约定

- **调用方式**：`import { invoke } from "@tauri-apps/api/core"`，`invoke<ReturnType>("命令名", { 参数 })` 返回 `Promise<ReturnType>`。
- **参数命名**：Rust 侧 `snake_case` 参数在 JS 侧自动映射为 `camelCase`。例：`paper_id` → `paperId`、`top_k` → `topK`、`new_settings` → `newSettings`、`source_path` → `sourcePath`。
- **错误处理**：所有命令返回 `Result<T, String>`，即 JS 侧 `Promise<T>`，失败时 reject 一个**字符串错误信息**（直接 `String(e)` 展示即可）。
- **可选参数**：`Option<T>` 形参在 JS 侧传 `null` 或不传即可（均视为「无」）。
- **时间戳**：`created_at` / `updated_at` / `last_read_at` 均为 **Unix 秒**，前端展示需 `new Date(ts * 1000)`。
- **页码**：`page_idx` 为 **0-based**（0 = 第 1 页）。
- **图片路径**：见 `get_paper_md` 说明。

---

## 1. 数据模型（TS 类型）

```ts
// ===== 设置 =====
interface ApiKeys {
  mineru: string;
  openai: string;
  anthropic: string;
  gemini: string;
  deepseek: string;
}

interface Settings {
  api_keys: ApiKeys;
  paper_library_path: string | null; // null = 默认应用数据目录
  embedding_model: string;           // 本地 embedding 模型名
  llm_provider: string;              // 见 §4 枚举
  llm_model: string;                 // 默认 LLM 模型名
}

// ===== 论文 =====
interface Paper {
  id: string;
  title: string;
  authors: string | null;      // 当前恒为 null（尚未提取作者）
  abstract: string | null;
  pdf_path: string;            // 本地绝对路径
  md_path: string;             // 解析出的 Markdown 绝对路径
  blog_md_path: string | null; // AI 博客绝对路径（生成前为 null）
  created_at: number;          // Unix 秒
  last_read_at: number | null;
  reading_status: string;
  parse_status: string;        // 见 §4 枚举
  folder_ids: string[];        // 所属文件夹 id 列表（多归属；空数组 = 未分类）
}

// ===== 文件夹（虚拟整理容器，不对应磁盘目录） =====
interface Folder {
  id: string;
  name: string;
  parent_id: string | null;    // 父文件夹 id；null = 顶级
  color: string;               // 色板 key：red/orange/yellow/green/teal/blue/purple/pink/brown/gray
  tags: string[];              // 自由文本标签
  created_at: number;          // Unix 秒
}

// ===== 检索 =====
interface SearchHit {
  chunk_id: number;            // paper_chunks 主键
  paper_id: string;
  paper_title: string;         // 跨论文检索时用于标识来源
  section: string;             // 所属章节（如 "Introduction"）
  content: string;             // 命中文本块全文
  page_idx: number | null;     // 0-based
  distance: number;            // 向量距离，越小越相关
}

// ===== 问答 =====
interface Citation {
  index: number;               // 对应回答正文中的 [n]，从 1 开始
  chunk_id: number;
  paper_id: string;
  paper_title: string;
  section: string;
  page_idx: number | null;
  snippet: string;             // 原文摘要（截断 ~200 字符）
}

interface QaMessage {
  role: "user" | "assistant";  // 会话中仅存这两种（system 不入库）
  content: string;
  citations?: Citation[] | null; // 仅 assistant 消息携带
}

interface Answer {
  conversation_id: string;     // 所属会话 id，可传给下一次 ask_question 续接多轮
  answer: string;              // 带 [n] 引用标记的 Markdown 文本
  citations: Citation[];       // 结构化引用，与 answer 中的 [n] 一一对应
}

// ===== 会话 =====
interface Conversation {
  id: string;
  paper_id: string | null;     // 跨论文问答为 null
  type: string;                // "qa" | "feynman"（当前仅有 "qa"）
  title: string;               // 由首条问题截断生成
  messages: string;            // JSON 字符串，JSON.parse 后为 QaMessage[]
  created_at: number;
  updated_at: number;
}
```

---

## 2. 命令清单

### 2.1 设置

#### `get_settings`
```ts
const s = await invoke<Settings>("get_settings");
```
无参数。读取 `settings.json`。

#### `update_settings`
```ts
const s = await invoke<Settings>("update_settings", { newSettings });
```
入参 `newSettings: Settings`（整体覆盖）。保存并返回保存后的 Settings。

---

### 2.2 论文

#### `list_papers`
```ts
const papers = await invoke<Paper[]>("list_papers");
```
无参数。按 `created_at` 倒序返回全部论文。

#### `get_paper`
```ts
const p = await invoke<Paper>("get_paper", { paperId });
```
入参 `paperId: string`。

#### `get_paper_md`
```ts
const md = await invoke<string>("get_paper_md", { paperId });
```
入参 `paperId: string`。返回论文 Markdown 全文，且把 MinerU 输出的相对图片路径 `](images/...` 重写为**本地绝对路径**，前端用 `convertFileSrc` 即可加载（参见现有 `Reader.tsx` 的 `resolveImgSrc`）。

#### `import_pdf`
```ts
const p = await invoke<Paper>("import_pdf", { sourcePath });
```
入参 `sourcePath: string`（本地 PDF 绝对路径，通常来自 `@tauri-apps/plugin-dialog` 的 `open()`）。把 PDF 复制进论文库并插入记录，返回 `parse_status = "unparsed"` 的 Paper。

#### `parse_pdf`
```ts
const p = await invoke<Paper>("parse_pdf", { paperId });
```
入参 `paperId: string`。**异步**。调用 MinerU 云解析 → 落盘 `paper.md` + 图片 + 结构化 JSON → 提取元数据 → 自动建立向量索引 → 返回 `parse_status = "ready"` 的 Paper。需要已配置 MinerU API Key。

#### `delete_paper`
```ts
await invoke<void>("delete_paper", { paperId });
```
入参 `paperId: string`。**不可逆**。级联删除：向量索引（vec_chunks）、文本分块（paper_chunks）、该论文的问答会话（conversations）、papers 行，以及磁盘上整个 `{uuid}/` 目录（PDF / Markdown / 图片 / AI 博客）。

---

### 2.3 检索 / 索引

#### `index_paper`
```ts
const chunkCount = await invoke<number>("index_paper", { paperId });
```
入参 `paperId: string`。手动重建单篇论文的向量索引，返回 chunk 数量（重索引幂等：删旧插新）。正常流程由 `parse_pdf` 自动完成，此命令用于手动补索引。

#### `search`
```ts
const hits = await invoke<SearchHit[]>("search", { query, topK, paperId });
```
| 参数 | 类型 | 说明 |
|------|------|------|
| `query` | `string` | 检索文本（中文/英文均可，会被向量化） |
| `topK` | `number` | 返回前 K 条 |
| `paperId` | `string \| null` | 可选；传 null/不传 = 跨论文，传值 = 限定该论文 |

返回按相关度降序（`distance` 升序）的命中列表。

---

### 2.4 AI 博客生成

#### `generate_blog`
```ts
const blogMd = await invoke<string>("generate_blog", { paperId, level });
```
| 参数 | 类型 | 说明 |
|------|------|------|
| `paperId` | `string` | 需已解析（有 `md_path`） |
| `level` | `"popular" \| "intro" \| "expert"` | 博客层级，见 §4 |

**异步**。读论文 Markdown → 调 LLM 生成分层博客 → 落盘 `blog.md` → 回写 `paper.blog_md_path` → 返回博客 Markdown 文本。需要已配置所选 provider 的 API Key。

---

### 2.5 RAG 知识库问答

#### `ask_question`
```ts
const ans = await invoke<Answer>("ask_question", {
  question, paperId, conversationId, topK,
});
```
| 参数 | 类型 | 说明 |
|------|------|------|
| `question` | `string` | 本轮问题 |
| `paperId` | `string \| null` | 可选；null = 跨论文检索，值 = 限定单篇 |
| `conversationId` | `string \| null` | 可选；null = 新建会话，值 = 续接多轮（历史进 LLM 上下文） |
| `topK` | `number \| null` | 可选；默认 5 |

**异步**。检索 → 组装上下文 → LLM 生成带 `[n]` 引用的回答 → 持久化到 `conversations`。返回 `Answer`（含 `conversation_id`、`answer`、`citations`）。

> 多轮：把上一次返回的 `conversation_id` 传入即可；`citations` 的 `[n]` 编号是每轮独立的。

#### `list_conversations`
```ts
const convs = await invoke<Conversation[]>("list_conversations");
```
无参数。按 `updated_at` 倒序返回全部 `type = "qa"` 会话（`messages` 为 JSON 字符串）。

#### `get_conversation`
```ts
const conv = await invoke<Conversation>("get_conversation", { conversationId });
```
入参 `conversationId: string`。读回单条会话（含完整 `messages` JSON 字符串）。

---

### 2.6 论文整理（虚拟文件夹）

> 多归属集合式语义：一篇论文可同时属于多个文件夹；「拖入文件夹」= **添加归属**，不是移动。
> 删除文件夹**不会删除论文**（论文失去该归属，无任何归属时出现在「未分类」）。
> 磁盘文件结构不变，文件夹关系全部存于 SQLite（`folders` + `paper_folders` 表）。

#### `list_folders`
```ts
const folders = await invoke<Folder[]>("list_folders");
```
无参数。按创建时间正序返回全部文件夹（扁平，前端自组树）。

#### `create_folder`
```ts
const folder = await invoke<Folder>("create_folder", { name, parentId, color, tags });
```
| 参数 | 类型 | 说明 |
|------|------|------|
| `name` | `string` | 文件夹名（trim 后非空；同级下不允许重名） |
| `parentId` | `string \| null` | 父文件夹 id；null = 顶级 |
| `color` | `string \| null` | 色板 key；null = `"gray"` |
| `tags` | `string[] \| null` | 标签列表；null = 空 |

#### `update_folder`
```ts
const folder = await invoke<Folder>("update_folder", { folderId, name, color, tags });
```
| 参数 | 类型 | 说明 |
|------|------|------|
| `folderId` | `string` | 目标文件夹 |
| `name` | `string \| null` | 新名称（null = 保持不变） |
| `color` | `string \| null` | 新颜色（null = 保持不变） |
| `tags` | `string[] \| null` | 新标签（null = 保持不变） |

#### `delete_folder`
```ts
await invoke<void>("delete_folder", { folderId });
```
删除文件夹：`paper_folders` 级联清除，**论文数据不删**；子文件夹上移一级（父变为被删文件夹的父，顶级则为 null）。

#### `add_papers_to_folder`
```ts
const added = await invoke<number>("add_papers_to_folder", { paperIds, folderId });
```
把多篇论文加入某文件夹（幂等：已存在的 no-op）。返回实际新增条数。

#### `remove_papers_from_folder`
```ts
const removed = await invoke<number>("remove_papers_from_folder", { paperIds, folderId });
```
把多篇论文从某文件夹移除归属（不删除论文）。返回实际移除条数。

#### `rename_paper`
```ts
const paper = await invoke<Paper>("rename_paper", { paperId, newTitle });
```
重命名论文（仅更新 `title` 元数据；磁盘 PDF/MD 文件名不动）。`newTitle` trim 后非空校验。

---

## 3. 本地文件布局

每篇论文一个目录（`paper_library_path` 或默认 `<app_data>/papers/{uuid}/`）：

```
{uuid}/
├── paper.pdf            # 原始 PDF
├── paper.md             # MinerU 解析结果（全文 Markdown）
├── blog.md              # AI 博客（generate_blog 后生成）
├── images/              # MinerU 返回的图片（get_paper_md 已重写为绝对路径）
└── *_content_list.json  # 结构化语义块（供分块索引）
```

---

## 4. 枚举值

| 字段 | 取值 |
|------|------|
| `parse_status` | `"unparsed"` \| `"parsing"` \| `"ready"` \| `"failed"` |
| `llm_provider` | `"openai"` \| `"anthropic"` \| `"gemini"` \| `"deepseek"` |
| `generate_blog.level` | `"popular"`（科普版）\| `"intro"`（入门版）\| `"expert"`（专业速读版） |
| `Conversation.type` | `"qa"`（当前唯一，`"feynman"` 为 Phase 5 预留） |

---

## 5. 前端接入示例（可直接复制）

```ts
import { invoke } from "@tauri-apps/api/core";

export type BlogLevel = "popular" | "intro" | "expert";

export const indexPaper = (paperId: string) =>
  invoke<number>("index_paper", { paperId });

export const search = (query: string, topK: number, paperId?: string | null) =>
  invoke<SearchHit[]>("search", { query, topK, paperId: paperId ?? null });

export const generateBlog = (paperId: string, level: BlogLevel) =>
  invoke<string>("generate_blog", { paperId, level });

export const askQuestion = (
  question: string,
  opts?: { paperId?: string | null; conversationId?: string | null; topK?: number },
) =>
  invoke<Answer>("ask_question", {
    question,
    paperId: opts?.paperId ?? null,
    conversationId: opts?.conversationId ?? null,
    topK: opts?.topK,
  });

export const listConversations = () =>
  invoke<Conversation[]>("list_conversations");

export const getConversation = (conversationId: string) =>
  invoke<Conversation>("get_conversation", { conversationId });
```

> 现有 `src/lib/api.ts` 已含 `getSettings` / `updateSettings` / `listPapers` / `getPaper` / `getPaperMd` / `importPdf` / `parsePdf`，新增命令按上表补齐即可。
