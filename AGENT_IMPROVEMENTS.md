# AI Agent 工具调用优化方案实施报告

## 问题背景

在 AI 问答功能中，当 AI 调用工具过多时会出现回答失败的问题，主要发生在复杂问题需要查阅多个章节的场景。

### 原始问题诊断

1. **协议违规问题（最严重）**
   - 当模型一次返回超过 8 个工具调用时，只执行前 8 个
   - 超出部分被静默丢弃，未注入 `ToolResult`
   - 违反协议要求（每个 tool_call_id 必须有对应的 result）
   - 导致 LLM 混乱或生成失败

2. **步数限制过低**
   - `MAX_STEPS = 6`：单轮最多 6 次模型调用
   - 对复杂研究任务不足，容易耗尽进入兜底回答

3. **缺乏分阶段指导**
   - System prompt 鼓励使用多个工具但未明确限制
   - 没有引导模型分步执行的策略

## 解决方案

参考 DeepSeek harness 等开源 agent 工具的设计理念，采用**分层防御策略**。

### 1. 修复协议违规 ✅

**修改位置**: `src-tauri/src/agent/mod.rs:509-537`

**核心逻辑**:
```rust
// 执行完前 MAX_TOOLS_PER_TURN 个工具后，为超限的工具调用注入错误结果
if calls.len() > MAX_TOOLS_PER_TURN {
    for call in calls.iter().skip(MAX_TOOLS_PER_TURN) {
        // 注入 ToolResult 到 messages（协议完整性）
        messages.push(AgentMsg::ToolResult {
            call_id: call.id.clone(),
            name: call.name.clone(),
            content: "工具执行失败：本轮已执行 8 个工具（达到上限），请在下一轮继续调用此工具...",
        });
        // 记录到 trace（前端展示）
        trace.push(ToolStep { ... });
        // 发送 ToolEnd 事件（实时反馈）
        sink(AgentEvent::ToolEnd { ... });
    }
}
```

**效果**:
- 保证协议完整性：每个 tool_call_id 都有对应的 result
- 错误消息明确提示"请下一轮继续"，引导模型分步执行
- 前端可见工具被限制的情况

### 2. 提升执行能力 ✅

**修改位置**: `src-tauri/src/agent/mod.rs:35-38`

**参数调整**:
```rust
// 修改前
pub const MAX_STEPS: usize = 6;
const MAX_TOOLS_PER_TURN: usize = 8;

// 修改后
pub const MAX_STEPS: usize = 15;  // 6 → 15
const MAX_TOOLS_PER_TURN: usize = 8;  // 保持不变，作为安全上限
```

**理由**:
- 参考 DeepSeek harness（通常 10-20 步）
- 支持复杂多轮研究任务
- MAX_TOOLS_PER_TURN 保持 8 作为单轮安全上限，防止失控

### 3. 优化 System Prompt ✅

**修改位置**: `src-tauri/src/agent/mod.rs:563-589`

**新增"工具调用策略"段**:
```
工具调用策略：
- 每轮最多调用 8 个工具，超出部分会被延迟到下一轮；
- 对于复杂问题，优先调用最核心的工具（如先检索概览，再精读关键章节），逐步深入；
- 避免一次性调用过多工具，建议分 2-3 轮完成深度研究；
- 如果某轮工具被限制，系统会提示你在下一轮继续。
```

**保留原有指导**:
- 工作流程建议（添加"分阶段执行"标注）
- 澄清说明、引用规则、公式格式等

## 测试验证

### 1. 现有测试全部通过 ✅

运行 16 个单元测试，全部通过：
```bash
cargo test --lib agent::tests
# test result: ok. 16 passed; 0 failed
```

### 2. 新增专项测试 ✅

#### 测试 A: `tools_over_limit_get_error_results`
验证工具超限时的协议修复：
- 模型一次调用 10 个工具
- 前 8 个正常执行（无错误）
- 后 2 个收到错误结果（标记超限）
- 模型能继续生成回答

#### 测试 B: `model_can_continue_after_tool_limit_error`
验证多轮执行能力：
- 第一轮调用 10 个工具（8 个成功 + 2 个超限）
- 第二轮模型理解错误提示，只调用 2 个工具
- 最终生成完整回答

### 3. 测试用例修复 ✅

修复 `max_steps_exhausted_produces_fallback` 测试：
- 使用 `MAX_STEPS` 常量替代硬编码的 6
- 确保测试随配置更新自动适配

## 预期效果对比

### 修改前
❌ 复杂问题 → 一次调用 10+ 工具 → 超出 8 个被静默丢弃 → 协议错误 → 生成失败  
❌ 6 步耗尽 → 兜底回答"未能整理出完整回答"

### 修改后
✅ 复杂问题 → 第一轮 8 个工具 → 剩余工具收到"下轮继续"提示 → 第二轮继续 → 最多 15 轮 → 完整回答  
✅ 模型学会分阶段：先概览 → 再精读 → 最后综合

## 实际应用场景

### 场景 1: 复杂对比分析
**用户提问**: "详细对比论文的方法部分与相关工作，并分析实验结果"

**执行流程**:
1. 第一轮：调用 `get_outline` + `get_paper_meta` + `search_papers`（3-4个工具）
2. 第二轮：精读方法章节 `read_section` × 2-3
3. 第三轮：精读相关工作 `read_section` × 2-3
4. 第四轮：分析实验结果 + 综合回答

### 场景 2: 多章节查阅
**用户提问**: "这篇论文的创新点是什么？包括方法、实验、结论的创新"

**执行流程**:
1. 第一轮：获取概览和元数据
2. 第二轮：查阅引言和方法章节（每轮 ≤ 8 个工具）
3. 第三轮：查阅实验和结论章节
4. 综合所有信息生成回答

## 风险评估与监控

### 低风险 ✅
- 协议修复是纯增量（只为之前被丢弃的调用补结果）
- MAX_STEPS 提升不影响快速问答（1-2 轮即完成）

### 中风险 ⚠️
- 15 轮可能增加复杂问题的响应时间
- **缓解措施**: 前端已有"暂停"功能，用户可中断

### 需要监控的指标 📊
1. **平均步数使用量**: 观察是否真的需要 15 步（可能大多数问题 ≤ 5 步）
2. **工具超限频率**: 观察 MAX_TOOLS_PER_TURN=8 是否需要调整
3. **响应时间**: 对比修改前后的平均响应时间
4. **成功率**: 对比修改前后的问答成功率（减少"未能整理出完整回答"）

## 后续优化建议

如果实际运行效果良好，可考虑：

1. **动态步数限制**
   - 简单问题自动降低（如只调用 1-2 个工具，限制 5 步）
   - 复杂问题自适应提升（检测到多次工具调用，扩展到 20 步）

2. **工具优先级**
   - 高优先级工具（`search_papers`, `get_outline`）优先执行
   - 低优先级工具在接近限制时延迟

3. **成本控制**
   - 记录每次对话的 token 消耗
   - 设置预算上限（如单次对话不超过 50k tokens）
   - 接近上限时主动触发总结

4. **智能工具选择**
   - 根据问题类型自动推荐工具组合
   - 学习用户偏好（某些用户更喜欢详细分析，某些更喜欢快速概览）

## 修改文件清单

- `src-tauri/src/agent/mod.rs`
  - 行 35-38: MAX_STEPS 提升 6 → 15
  - 行 369-537: 添加工具超限协议修复逻辑
  - 行 563-589: 优化 system prompt，新增工具调用策略
  - 行 808-831: 修复测试用例适配新 MAX_STEPS
  - 行 1207-1309: 新增 2 个专项测试

## 实施日期

2024-09-16

## 测试状态

✅ 所有测试通过 (16/16)
✅ 协议修复验证通过
✅ 多轮执行验证通过

---

**建议**: 上线后监控 1-2 周，收集实际数据后根据需要微调参数。
