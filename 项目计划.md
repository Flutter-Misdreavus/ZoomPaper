# 设计文档

> 个人本地优先的 macOS 论文阅读与知识管理平台。

---

## 1. 产品定位

- **目标用户**：个人研究者
- **核心闭环**：上传 → AI 解读 → 知识库构建 → 费曼输出
- **设计原则**：数据本地存储，云端仅作算力外包（LLM / Embedding / MinerU）

---

## 2. 技术架构

| 层级 | 选型 | 说明 |
|------|------|------|
| 桌面框架 | **Tauri v2** | Rust 后端 + Web 前端，原生 `.app` 打包 |
| 前端 | React 18 + TypeScript + Tailwind + shadcn/ui | 学术风 UI |
| 本地数据库 | **SQLite** (`rusqlite`) | 单文件，备份即拷贝 |
| 向量检索 | **sqlite-vec** | SQLite 扩展，零外部依赖 |
| PDF 解析 | **MinerU API** | PDF → Markdown，云端转换后存本地 |
| LLM / Embedding | 用户自选 API Key | OpenAI / Anthropic / Gemini / DeepSeek |

---

## 3. 本地文件组织

```
~/Library/Application Support/com.paper-reader/
├── database.sqlite          # 结构化数据 + 向量
├── settings.json            # API Keys、用户偏好
└── papers/                  # 论文库（用户可自定义路径）
    └── {uuid}/
        ├── paper.pdf
        ├── paper.md         # MinerU 输出
        └── blog.md          # AI 生成博客
```

---

## 4. 数据模型

### 4.1 论文表
```sql
CREATE TABLE papers (
    id TEXT PRIMARY KEY,           -- UUID
    title TEXT NOT NULL,
    authors TEXT,                  -- JSON
    abstract TEXT,
    pdf_path TEXT NOT NULL,
    md_path TEXT NOT NULL,
    blog_md_path TEXT,
    created_at INTEGER,
    last_read_at INTEGER,
    reading_status TEXT DEFAULT 'unread'
);
```

### 4.2 文本分块（RAG）
```sql
CREATE TABLE paper_chunks (
    id INTEGER PRIMARY KEY,
    paper_id TEXT REFERENCES papers(id),
    section TEXT,                  -- abstract / intro / method / results / conclusion
    content TEXT NOT NULL,
    start_line INTEGER,
    end_line INTEGER
);

-- 向量虚拟表（sqlite-vec）
CREATE VIRTUAL TABLE vec_chunks USING vec0(
    embedding float[1536]          -- text-embedding-3-small
);
```

### 4.3 对话历史
```sql
CREATE TABLE conversations (
    id TEXT PRIMARY KEY,
    paper_id TEXT REFERENCES papers(id),
    type TEXT CHECK(type IN ('qa', 'feynman')),
    title TEXT,
    messages TEXT NOT NULL,        -- JSON 数组
    created_at INTEGER,
    updated_at INTEGER
);
```

---

## 5. 核心功能模块

### 5.1 论文上传与解析
1. 用户选择 PDF → 复制到本地论文库
2. 调用 MinerU API → 返回 Markdown
3. 解析 MD 提取元数据（title / authors / abstract）
4. 按论文章节分块 → 生成 Embedding → 存入 sqlite-vec

### 5.2 AI 博客生成
- 输入：论文 Markdown 全文
- 输出：分层的通俗博客文章
- 层级：科普版 / 入门版 / 专业速读版（可选）
- 结构：核心问题 → 方法直觉 → 结果意义 → 局限延伸

### 5.3 知识库问答（RAG）
1. 用户提问 → Embedding 向量化
2. sqlite-vec 跨论文检索 Top-K 段落
3. 组装上下文 Prompt → 调 LLM 生成回答
4. 回答带引用标记，点击可定位原文

### 5.4 费曼学习法（多轮 Agent）
- AI 角色：聪明但陌生的本科生
- 交互：用户教授 → AI 追问 / 要求类比
- 评估维度：简洁度、准确性、直觉性
- 输出：教学复盘 + 概念关系图（预留）

---

## 6. Rust 后端模块划分

```
src-tauri/src/
├── main.rs
├── db/           # SQLite 建表、迁移、查询
├── fs/           # 论文目录管理、文件读写
├── ai/
│   ├── llm.rs    # 统一 LLM 接口（多 Provider）
│   ├── embed.rs  # Embedding 生成
│   └── mineru.rs # MinerU API 调用
├── rag/          # 分块策略、向量检索、上下文组装
└── feynman/      # 费曼对话状态机（预留）
```

---


*文档版本: v1.0*
*日期: 2026-08-13*
